{
  "name": "scamshield-honeypot-root",
  "version": "1.0.2",
  "description": "Root package for ScamShield Honeypot deployment",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}